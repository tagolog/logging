namespace Tagolog.NLog.Factory
{
    /// <summary>
    /// SQL providers supported by database target.
    /// </summary>
    enum SqlProviderSupported
    {
        SqlClient,
        Npgsql
    }
}
